import { spacing } from './theme';

/** Gap between top-level sections on the Homepage */
export const SECTION_GAP = spacing['3xl'];       // 40

/** Gap between a section header (eyebrow + title) and its content */
export const SECTION_HEADER_GAP = spacing.xl;    // 24

/** Gap between cards within a section */
export const CARD_GAP = spacing.lg;              // 16

/** Horizontal content padding (left + right inset) */
export const CONTENT_PADDING_H = spacing.xl;     // 24
